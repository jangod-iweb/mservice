<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://dubbo.apache.org/schema/dubbo http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="${dubbo.application.name}" />
    <!-- 注册中心暴露服务地址 -->
    <dubbo:registry protocol="${dubbo.registry.protocol}" address="${dubbo.registry.address}" />
    <dubbo:consumer check="false" retries="0" timeout="30000" group ="${dubbo.registry.group}"/>

    <!--暴露服务-->
    <dubbo:protocol name="dubbo" port="${dubbo.protocol.port}" dispather="all" threadpool="fixed" threads="20"/>
    <dubbo:reference id="pluginService" interface="org.jangod.iweb.mapp.service.PluginService" group="${app.registry-group}" />
    <dubbo:reference id="bspSSOService"  interface="org.jangod.iweb.mapp.service.BspSSOService" group="${app.registry-group}" />
</beans>